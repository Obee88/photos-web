# Build-time fallback (used by Vite dev server and as a fallback if API_URL is
# not set at runtime). For production, set API_URL as a container env var
# instead — no rebuild required.
VITE_API_URL=http://localhost:8080
